#include "monty.h"
/**
 * f_stack - prints stack top
 * @head: head of stack
 * @counter: number of lines counted
 * Return: nothing to return
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
